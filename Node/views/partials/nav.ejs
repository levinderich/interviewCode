<nav class="navbar navbar-default navbar-static-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="<% if (title === 'Movies') { %>#<% } else { %>/<% } %>">Maverick Inc.</a>
    </div>
    <ul class="nav navbar-nav">
      <li <% if (title === 'Movies' || title === 'Movie') { %>class="active" <% } %> ><a href="/">Movies</a></li>
      <li <% if (title === 'Bookings') { %>class="active" <% } %> ><a href="/bookings">Bookings</a></li>
    </ul>
  </div>
</nav>
